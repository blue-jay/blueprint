{
	"config.type": "collection",
	"config.collection": [
		{
			"model/nameonly": {
				"package": "{{.model}}",
				"table": "{{.model}}"
			}
		},
		{
			"controller/nameonly": {
				"package": "{{.controller}}",
				"url": "{{.controller}}",
				"model": "{{.model}}",
				"view": "{{.view}}"
			}
		},
		{
			"view/default": {
				"model": "{{.view}}"
			}
		}
	],
	"model": "",
	"controller": "",
	"view": ""
}