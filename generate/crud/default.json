{
	"config.type": "collection",
	"config.collection": [
		{
			"model/default": {
				"package": "{{.controller}}",
				"table": "{{.model}}"
			}
		},
		{
			"controller/default": {
				"package": "{{.controller}}",
				"url": "{{.controller}}",
				"model": "{{.model}}",
				"view": "{{.view}}"
			}
		}
	],
	"model": "",
	"controller": "",
	"view": ""
}