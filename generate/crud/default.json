{
	"config.type": "collection",
	"config.collection": [
		{
			"model/default": {
				"package": "{{.model}}",
				"table": "{{.model}}"
			}
		},
		{
			"controller/default": {
				"package": "{{.controller}}",
				"url": "{{.controller}}",
				"model": "{{.model}}",
				"view": "{{.view}}"
			}
		},
		{
			"view/create": {
				"model": "{{.view}}"
			}
		},
		{
			"view/edit": {
				"model": "{{.view}}"
			}
		},
		{
			"view/index": {
				"model": "{{.view}}"
			}
		},
		{
			"view/show": {
				"model": "{{.view}}"
			}
		}
	],
	"model": "",
	"controller": "",
	"view": ""
}